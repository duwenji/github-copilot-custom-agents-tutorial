# PUBLISHING

## Release checklist overview
1. `npm install` を実行して依存関係を揃える
2. `npm run quiz:validate` を実行する
3. `npm run ebook:build` を実行して `ebook-output/` を確認する
4. GitHub Pages の表示とリンク導線を確認する
5. リリースノートと `CHANGELOG.md` を更新する
